# Django Secret Key - Generate a new one with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here

# Database (optional - uses SQLite by default, PostgreSQL when ON_HEROKU is set)
# MONGODB_URI was used in the old Express version - no longer needed for Django
# DATABASE_URL=postgresql://user:password@localhost/dbname

# Email Configuration (optional - only needed if you want email verification)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password

# Social Auth (optional - configure in Django Admin after creating Social Applications)
# Google OAuth
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_SECRET=your-google-secret

# GitHub OAuth
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_SECRET=your-github-secret

# Heroku sets this automatically - no need to set locally
# ON_HEROKU=False
